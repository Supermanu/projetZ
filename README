Instruction pour l'installation :
Il faut Geant4, make et g++ ou équivalent. Cmake est facultatif et s'emploie
seulement si la version de Geant4 est >= 9.5.

Pour installer cmake et make sur ubuntu:
Dans un terminal: sudo apt-get install cmake make


Pour compiler:

make

Si on veut utiliser cmake, il suffit de faire
cmake .
make

Si on utilise une version >= 9.5, on peut décommenter un #define dans
projetZPhysicsList ; la version 9.5 propose une façon plus simple d'introduire
les particules utilisisées.


Pour installer et configurer git sur linux:

Dans un terminal taper: sudo apt-get install git
Ensuite, il faut configurer sa clé ssh. Le lien suivant explique très bien (2e
partie de la page)
http://help.github.com/linux-set-up-git/

Pour utiliser notre dépôt, il faut se placer dans un terminal à l'endroit où on
veut que le code source se trouve:

	cd ~/mon/endroit/préféré/

et rentrer la commande suivante:

	git clone git@github.com:Supermanu/projetZ.git

Ensuite pour utiliser git, les commandes principales sont:

	git pull

Pour récupérer les modifications faites par les autres.

	git add Le_nom_du_fichier_ou_du_dossier

Pour ajouter un fichier ou un dossier.

	git commit -a
	git push

Pour envoyer les modifications et l'ajout de fichiers.

